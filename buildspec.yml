version: 0.2

phases:
  install:
    commands:
      - php -v
  build:
    commands:
      - date
artifacts:
  files:
    - '**/*'
  base-directory: base_dir
  name: build-artifact-name
  discard-paths: no
